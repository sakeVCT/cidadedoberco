backend:
  name: git-gateway
  branch: main

media_folder: "static/images"
public_folder: "/images"

collections:
  - name: "empresas"
    label: "Empresas"
    folder: "content/empresas"
    create: true
    slug: "{{slug}}"
    summary: "{{data_pagamento}} — {{title}}"
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - {label: "Categoria", name: "category", widget: "string"}
      - {label: "Estatuto Premium", name: "premium", widget: "boolean", default: false}
      - {label: "Data de Inscrição", name: "data_pagamento", widget: "datetime", format: "YYYY-MM-DD", date_format: "DD/MM/YYYY", time_format: false}
      # ESTE É O CAMPO NOVO QUE PRECISAS:
      - {label: "Telemóvel/WhatsApp", name: "whatsapp", widget: "string", required: false, hint: "Ex: 912345678"}
      - {label: "Email do Cliente", name: "email_cliente", widget: "string", required: false}
      - {label: "Imagem", name: "image", widget: "image"}
      - {label: "Morada", name: "address", widget: "string"}
      - {label: "Corpo do Texto", name: "body", widget: "markdown"}
